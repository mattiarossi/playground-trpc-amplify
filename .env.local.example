# Database Connection
# PostgreSQL connection string for Drizzle ORM
AMPLIFY_DATABASE_URL=postgresql://username:password@localhost:5432/blog_db

# Amplify Backend Configuration (Optional)
# These are used during Amplify backend deployment/build time

# VPC Configuration for Lambda functions (JSON string)
# Only needed if Lambda needs to access resources in a VPC (e.g., RDS in private subnet)
# AMPLIFY_VPC_CONFIG='{"SecurityGroupIds":["sg-aaaaaaa33248baaaa"],"SubnetIds":["subnet-aaaaaae65be8aaaa"]}'

# Database URL for Lambda functions (alternative to SSM Parameter Store)
# AMPLIFY_DATABASE_URL=postgresql://username:password@rds-endpoint:5432/production_db

# Environment Name (optional, defaults to 'trpcSandBox')
# ENV_NAME=development

# Next.js Frontend Configuration (Optional)
# These are automatically generated in amplify_outputs.json after deployment
# Only set manually if not using Amplify CLI or for custom configuration

# NEXT_PUBLIC_APPSYNC_EVENTS_ENDPOINT=wss://your-events-api.appsync-api.us-east-1.amazonaws.com/event
# NEXT_PUBLIC_AWS_REGION=us-east-1
